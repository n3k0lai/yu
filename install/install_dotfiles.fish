function install_dotfiles
    # args
    #   -a --auto
    #   -g --github
    #   -d --doom
    #   -o --os
    #   -g --graphical
    #   -h
    # ensure git is installed
    # download dotfiles
    # mv dotfiles
    # mv git folder
    # install yu
    # install deps
    # download doomdir
    # download doomemacs
    # check for find and ripgrep
    # install doom
    # download org
end
